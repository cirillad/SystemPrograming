<Window x:Class="ChatApp.MainWindow"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
     TextElement.Foreground="{DynamicResource MaterialDesignBody}"
     TextElement.FontWeight="Regular"
     TextElement.FontSize="13"
     TextOptions.TextFormattingMode="Ideal"
     TextOptions.TextRenderingMode="Auto"
     Background="{DynamicResource MaterialDesignPaper}"
     FontFamily="{DynamicResource MaterialDesignFont}"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Welcome" Height="100" Width="300" 
        ResizeMode="NoResize">
    <!-- Забороняємо змінювати розмір -->
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="49*"/>
            <ColumnDefinition Width="51*"/>
        </Grid.ColumnDefinitions>
        <!-- Виправлено формат кольору -->
        <Button Content="Login" Width="100" Height="30" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="30,0,0,0" Click="LoginButton_Click" />
        <Button Content="Register" Width="100" Height="30" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="23,0,0,0" Click="RegisterButton_Click" Grid.Column="1"/>
    </Grid>
</Window>
